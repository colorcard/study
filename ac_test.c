#include <stdio.h>

int main(){
    char *str,*num,*a;
    int i,k=0,j;
    scanf("heights=[%s]\n",str);

    for(i=0;str[i]!='\0';i++){
        for(j=0;str[i]!=',';j++){
            num[j]=str[i];
        }
        if(j!=0){
            a[k]=
        }

    }

    return 0;
}